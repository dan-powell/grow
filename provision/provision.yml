---
- name: Provision containers
  hosts: all
  roles:
    - role: autumnrain.docker.laravel
  tasks:
    - name: Run base site provision tasks
      ansible.builtin.include_role:
        name: autumnrain.docker.laravel
        tasks_from: base
